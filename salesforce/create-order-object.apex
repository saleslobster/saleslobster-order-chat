// This script creates the Order__c custom object programmatically
// Note: In production, use metadata API. This is a workaround for testing.
// For now, we'll use the manual approach via UI or use a proper deploy

System.debug('Order__c setup: Use the seed-data.apex after creating the object in Salesforce UI');
