{
  "name": "saleslobster-order-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod",
    "test": "node --test"
  },
  "dependencies": {
    "openai": "^4.52.0",
    "jsforce": "^3.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
